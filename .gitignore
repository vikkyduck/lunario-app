# секреты и данные — живут только на сервере
.env
data/
*.db
backups/
# тексты и картинки приложения — не код: на сервере это /opt/lunario-content, локально — рабочая копия.
# В git остаётся только описание формата
content/*
!content/ПРОЧТИ-МЕНЯ.txt
# cities.db собирается сам: node tools/build-cities.mjs (данные GeoNames, CC BY)
backend/cities.db

# мобильная оболочка (Capacitor) — отдельный проект: в этом репозитории не версионируется, на диске остаётся
mobile/
lunario-mobile.zip
*.jks
*.keystore

# системный мусор
.DS_Store
node_modules/
.local-preview/

# артефакты Xcode
mobile/ios/**/xcuserdata/
mobile/ios/build/
*.xcarchive

# локальные настройки Claude Code
.claude/
.deploy-snapshot
